[build-system]
requires = ["setuptools>=36.6.0", "pbr>=5.8.0"]
build-backend = "pbr.build"

[tool.black]
line-length = 88
target-version = ['py36']
skip-string-normalization = true
exclude = '''
(
  /(
      \.eggs
    | \.git
    | \.tox
    | \.venv
    | build
    | dist
  )
)
'''
